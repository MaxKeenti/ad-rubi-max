{
  "name": "mangos-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.14.0"
  }
}
