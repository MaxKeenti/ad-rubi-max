# Typst build artifacts — regenerated by `just build`.
# The final entregable PDFs are committed manually right before submission
# (or distributed out-of-band), not on every compile.
main.pdf
entrega-completa.pdf
